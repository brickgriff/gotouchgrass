{
  "name": "gotouchgrass",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode github",
    "preview": "vite preview",
    "build:github": "vite build --mode github",
    "build:itch": "vite build --mode itch"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
