const Random = (function (/*api*/) {
    var api = {};

    // take in a string
    // translate that as a number
    // use that as an angle in a sin call
    // take the decimal part
    // use that as your next number

    return api;
}());